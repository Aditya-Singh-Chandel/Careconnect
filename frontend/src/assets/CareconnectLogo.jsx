function CareconnectLogo({ fillColor }) {
  return (
    <div
      style={{
        color: fillColor || "#ffffff",
        fontSize: "1.5rem",
        fontWeight: "bold",
        cursor: "pointer",
      }}
    >
    </div>
  );
}

export default CareconnectLogo;